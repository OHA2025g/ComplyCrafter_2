# Feature Flags

`flags.example.json` illustrates how to toggle the ADT1 modern stack rollout. Copy it to `flags.json`, set `adt1_modern_stack.enabled` to `true`, and mount into the gateway/forms containers or your flag service.
