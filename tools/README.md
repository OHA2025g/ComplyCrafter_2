# Tooling

Scripts and CLIs supporting migration (code generation, data sync, regression diffs).

## Phase 0 Deliverables
- `migration_cli/` Python package with entrypoint stub.
- `smoke_tests/` folder for cross-stack comparison harness.
- Shared `.pre-commit-config.yaml` for formatting (Black, isort, prettier).
