# Service Scaffold

_Phase 0 placeholder._

## To-Do
- Initialize Poetry environment (`poetry init`) with FastAPI, uvicorn, SQLAlchemy, pydantic.
- Create main application entrypoint under `app/main.py` with healthcheck route.
- Add Dockerfile and Makefile.
- Write pytest smoke test hitting `/healthz`.
