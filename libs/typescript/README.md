# Shared TypeScript Libraries

Contains API clients, shared models, and Angular utilities published via Nx libs.

## Phase 0 Tasks
- Add `api-client/` generated via `openapi-typescript`.
- Provide `shared-utils/` for formatting, validation, and telemetry instrumentation.
- Configure lint + unit test targets through Nx.
