# API Client Generator

Run `npm run generate` (inside this folder) after exporting an OpenAPI spec from the FastAPI services. The generated file is committed to `src/generated.ts` and consumed by `frontend/libs/data-access`.
