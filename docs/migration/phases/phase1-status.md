# Phase 1 – ADT1 Pilot Status

Use this log to track progress through Phase 1.

## Completed
- Dual-run procedure documented (`docs/runbooks/dual-run.md`).
- DB migration & seeding guide added (`docs/runbooks/adt1_migration.md`).
- Monitoring & feature-flag rollout plan documented (`docs/ops/monitoring-flags.md`).

- Angular ADT1 form scaffolding (component, route, model) created.
- FastAPI ADT1 router, schema, service, and SQLAlchemy model implemented.
- Forms service tests cover create/list behavior with SQLite and sample data fixture.
- End-to-end mapping updated to reference new Angular/Python assets.
- Playwright spec + Jest test committed; migration SQL + feature flag config added.
- Migration CLI now supports `generate` and `dualrun`.

## Upcoming
- Ready for Phase 2 expansion (next form batch).
